# Журнал змін — «НЕ ЖАЛЬ»

## v2.1 — аудит пристроїв і мови (2026-09-05)
- Корінь мобільного бага знайдено й виправлено: початковий стан анімації
  штампа (`scale(1.9)`, opacity 0) виходив за межі картки → ширина документа
  432 px при екрані 375 → на Android фіксована шапка з'їжджала за екран.
  Виправлено через `main { overflow-x: clip }` + `.dossier { overflow: hidden }`.
- Тач-зони кнопок збільшено до ≥34 px на `(pointer: coarse)` (були 17–21 px).
- `:hover`-стилі обмежено `(hover: hover)` — на тачскрінах чип «джерело»
  більше не «залипає» кольором після тапу.
- Мінімальний розмір службових підписів піднято до 11 px (був 9,3–9,9 px).
- Виправлено z-index тегу «ДОСЬЄ», який ховався під растровим портретом.
- Фолбек `100vh` перед `100svh`; додано `<meta color-scheme=dark>` і `<noscript>`.
- Версіонування ресурсів `?v=NN` в `index.html` — Safari тримав старі CSS/JS
  у кеші навіть після оновлення HTML.
- Прибрано мертвий шар `.grain` (посилався на невизначену змінну).
- Адаптивні зображення: `assets/panels/sm/` (720 px) і `md/` (1080 px) із
  `srcset`/`sizes` — повний комікс на телефоні важить ≈2,4–3,7 МБ замість 10 МБ.
- Мова: виправлено жаргон, кальки, типографіку й неоднозначні формулювання
  у 25 місцях `js/data.js` і в одній схемі.
- Перевірено: горизонтального переповнення немає на 320–2560 px; шрифти з
  кирилицею завантажуються; 25 зображень завантажуються відкладено (lazy);
  консоль без помилок.

## v2.0 — фінальний растровий арт (2026-09-03)
- 25 WebP-панелей у `assets/panels/` (портрети каст, схеми, заставки глав,
  hero) поверх наявних кодових SVG, які лишаються фолбеком під кожним
  растром (`onerror`).
- Нові слоти: hero-фон із градієнтом читабельності, заставки глав (по одній
  на главу).
- Шляхи до панелей — через функцію `panel(name)`.
- Виправлено: кнопка «Рух» зрізалась на 390 px; положення заставок глав, щоб
  не обрізати верхню частину зображення.

## v1.1 — портрети касту як нуарні кадри (2026-08-24)
- `js/figures-cast.js` переписано: кожен портрет — панель-кадр (стіна з
  мугшот-лінійкою в реальних міліметрах, конус прожектора, тінь персонажа,
  віньєтка, halftone), а не симетрична фігурка на порожньому тлі.
- Крупне кадрування: черевце матки виходить за нижній край кадру.
- Мізансцени за кастою: матка — трон із почтом; трутень — мугшот; робоча —
  діагональ праці з пунктиром маршруту; скаут — нахил і крейдяна схема
  вісімки на стіні; Varroa — тінь кліща й контрове світло.
- Виправлено напрямок повороту крил і форму очей.

## v1.0 — візуальна перебудова (2026-08-22)
- Портрети касту стали параметричним генератором: усі касти — та сама
  *Apis mellifera* з різною морфологією, а не різним кольором. Матка —
  видовжене загострене черевце й почет; трутень — великі фасеткові очі й
  тупе черевце; робоча — кошик для пилку, хоботок, зазубрене жало; скаут —
  поза танцю-виляння; Varroa — плаский кліщ на хазяїні.
- Додано 14 пояснювальних схем-«доказів» у `js/figures-sci.js`, кожна
  прив'язана до конкретного твердження в `js/data.js`.
- `styles/animations.css` переписано: хореографія за реєстром замість
  однакового fade-up для всього — clip-path wipe для титрів, пауза між
  твердженням і спростуванням, малювання ліній схеми в порядку читання
  сенсу.
- Виправлено: ноги/вусики/жало, намальовані кольором `--ink`, були
  невидимі на темному тлі; шов halftone посеред черевця; `foreignObject`
  у схемі ботулізму не рендерився — замінено нативним SVG-текстом.

## v0.2 — переддоказовий прохід (2026-08-15)
- Доступність: sr-only live-регіон для оголошення зміни мови/руху,
  `prefers-reduced-data`.
- Дистрибуція: JSON-LD (Article), favicon, canonical, hreflang, OG/Twitter
  теги, растровий `og.png` 1200×630, `sitemap.xml`, `robots.txt`,
  `.nojekyll`, `netlify.toml`, `DEPLOY.md`.
- Наукова перевірка: усі 19 тверджень звірені з першоджерелами. Виправлено
  метадані п'яти джерел, що вказували не на ту статтю (`dwv`, `ceranaball`,
  `allergy`, `stingers`) і пом'якшено переоцінені числа (поріг Varroa,
  температура зимового клубу, формулювання про алергію).
- `node tools/check.mjs` зелений; усі DOI резолвляться в правильні статті
  через Crossref.

## v0.1 — перший робочий двомовний зріз (2026-08-14)
- Скелет і дизайн-система: дуотон чорний+бурштин, self-hosted шрифти
  Oswald + JetBrains Mono з кирилицею, зерно/halftone одним fixed-оверлеєм.
- Дані (`js/data.js`): 5 персонажів касту, 5 глав, 14 пар міф/наука,
  19 джерел — кожне твердження з DOI, обидві мови.
- Рушій (`js/main.js`): рендер з даних, перемикач UK/EN, поява карток при
  прокручуванні, розкривні панелі джерел, тумблер руху, перехід за якорем.
- Доступність: шлях reduced-motion і тумблер, skip-link, ARIA на
  чипах/перемикачах.
- `node tools/check.mjs` — машинний гейт цілісності (факт без DOI,
  неперекладений рядок, спірне твердження без картки-нюансу).
